

scalar Json


directive @require(
    arg: String
    field: String
    equalTo: String!
) on FIELD_DEFINITION | OBJECT

directive @needsLogin on FIELD_DEFINITION

type User  {
    name: String
}

type Query {
    me: User
}

